import search from 'penny-sae/utils/search';
import { module, test } from 'qunit';

module('Unit | Utility | search');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = search();
  assert.ok(result);
});
