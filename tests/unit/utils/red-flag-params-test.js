import redFlagParams from 'penny-sae/utils/red-flag-params';
import { module, test } from 'qunit';

module('Unit | Utility | red flag params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = redFlagParams();
  assert.ok(result);
});
