
import { concatHelper } from 'penny-sae/helpers/concat-helper';
import { module, test } from 'qunit';

module('Unit | Helper | concat helper');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = concatHelper('id-', 456789);
  assert.ok(result);
});

